Used Wikipedia Library for Python

I did try to go into the direction of scraping data but might be a better idea to utilize  a library if there is one already out there. 

Both of the inputs can be changed where top_words will be defaulting to 10 if not specified.